/**
 * IPC Input Validation Schemas
 *
 * This file re-exports from the modular validation folder for backwards compatibility.
 * New code should import directly from './validation/index' or specific domain files.
 *
 * @see ./validation/index.ts for the full export list
 * @see ./validation/utils.ts for validation utilities
 * @see ./validation/shared.ts for reusable schema components
 */

export * from './validation/index';
