
/**
 *
 * @param projectId - Current project ID
 */
  const {
    addUserMessage,

    if (!projectId) return;

    if (!projectId) return;

    // Backend cleanup happens in background - don't await
    });

}
