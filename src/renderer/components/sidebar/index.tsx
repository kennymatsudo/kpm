
interface SidebarProps {
  onDeleteProject?: () => void;
}

  return (
        {currentProjectId ? (
        ) : (
          <div className="p-6 text-center">
              </svg>
            </div>
            </p>
          </div>
        )}
      </div>
  );
